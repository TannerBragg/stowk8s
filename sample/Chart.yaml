apiVersion: v2
name: sample-app
description: A boilerplate Helm chart for regression testing
type: application
version: 0.1.0
appVersion: "1.0.0"

dependencies:
  - name: nginx-ingress
    version: "1.17.2"
    repository: "oci://public.ecr.aws/helm-charts"
    condition: nginx-ingress.enabled
